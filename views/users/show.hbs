<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>{{ user.username }}</title>
  </head>
  <body>
    <img src="{{ user.avatar_url }}" />
    <h3> {{ user.name }} - {{ user.username }}</h3>

    <a href="/users/{{ user.id }}/posts">Read Posts</a>
    <a href="/users/{{ user.id }}/comments">Read Comments</a>

    <h4><a href="/users">Go Back</a></h4>
  </body>
</html>
