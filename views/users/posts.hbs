<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>{{ user.username }}'s Posts</title>
  </head>
  <body>

    <h3>{{ user.username }}'s Posts</h3>

    {{#if posts.length}}
      <ul>
        {{#each posts}}
          <li>{{ this.title }} - {{ this.post }}</li>
        {{/each}}
      </ul>
    {{else}}
      <h4>User has no posts.</h4>
    {{/if}}


    <a href="/users/{{ user.id }}">Go Back</a>
  </body>
</html>
